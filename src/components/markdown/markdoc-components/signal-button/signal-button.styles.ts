import { type ButtonOverrides } from 'baseui/button';

export const overrides = {
  button: {
    BaseButton: {
      style: {
        margin: '2px',
      },
    },
  } satisfies ButtonOverrides,
};
